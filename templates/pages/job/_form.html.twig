{{ form_start(form) }}
<div class="row">
    <div class="col-md-12 mb-3">
        {{ form_row(form.name) }}
    </div>
</div>
{{ form_rest(form) }}
<button class="btn btn-primary">{{ button|default('Enregistrer') }}</button>
{{ form_end(form) }}
