{% extends 'base.html.twig' %}

{% block title %}Editer un utilisateur{% endblock %}

{% block body %}
    <div class="container">
        <a href="{{ path('utilisateurs.index') }}" class="btn btn-secondary">Retour</a>
        <h1>Editer un utilisateur</h1>
        {{ include('pages/utilisateurs/_form.html.twig', {'button': 'Editer'}) }}
    </div>
{% endblock %}
