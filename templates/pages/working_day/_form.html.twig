{{ form_start(form) }}
<div class="row">
    <div class="col-md-4 mb-3">
        {{ form_row(form.date) }}
    </div>
    <div class="col-md-4 mb-3">
        {{ form_row(form.isTeleworked) }}
    </div>
    <div class="col-md-4 mb-3">
        {{ form_row(form.user) }}
    </div>
</div>
<button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
